update RDB$RELATION_FIELDS set
RDB$NULL_FLAG = NULL
where (RDB$FIELD_NAME = 'ID_TECNICO_AGRICOLA') and
(RDB$RELATION_NAME = 'RECEITAS')

update RDB$FIELDS set
RDB$FIELD_TYPE = 10
where RDB$FIELD_NAME = 'RDB$28'

update RDB$FIELDS set
RDB$FIELD_TYPE = 10
where RDB$FIELD_NAME = 'RDB$22'

update RDB$FIELDS set
RDB$FIELD_TYPE = 10,
RDB$FIELD_LENGTH = 4,
RDB$CHARACTER_LENGTH = NULL
where RDB$FIELD_NAME = 'RDB$23'


ALTER TABLE PEDIDO_VENDA_TOTAL ALTER VALOT_TOTAL TO VALOR_TOTAL;

ALTER TABLE PEDIDO_VENDA_TOTAL DROP ID_RECEITA;

